---
- name: Configure VPS
  hosts: vps
  become: true
  vars_files:
    - vault.yml

  roles:
    - base
    - docker
    - traefik
    - dns
    - vaultwarden
    - monitoring
    - { role: jenkins, tags: ['jenkins'] }
