---
zot_dir: /opt/zot
zot_data_dir: "{{ zot_dir }}/data"
zot_image: "ghcr.io/project-zot/zot-linux-amd64:v2.1.16"
# zot_registry_user is defined in group_vars/all.yml so the Jenkins
# CasC template (rendered on a different host) can also read it.
