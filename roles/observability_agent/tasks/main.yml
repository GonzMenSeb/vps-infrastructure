---
- name: Create observability-agent directory
  file:
    path: "{{ observability_agent_dir }}"
    state: directory
    mode: "0755"

- name: Copy Alloy config
  template:
    src: alloy-config.alloy.j2
    dest: "{{ observability_agent_dir }}/alloy-config.alloy"
    mode: "0644"
  notify: restart observability_agent

- name: Copy docker-compose file
  template:
    src: docker-compose.yml.j2
    dest: "{{ observability_agent_dir }}/docker-compose.yml"
    mode: "0644"

- name: Start observability-agent stack
  community.docker.docker_compose_v2:
    project_src: "{{ observability_agent_dir }}"
    state: present
    pull: missing

- name: Wait for Alloy readiness
  uri:
    url: http://127.0.0.1:12345/-/ready
    status_code: 200
  register: alloy_ready
  retries: 10
  delay: 3
  until: alloy_ready.status == 200

# node_exporter (runs on the host, not in Docker)
- name: Check if node_exporter binary exists
  stat:
    path: /usr/local/bin/node_exporter
  register: node_exporter_bin

- name: Download node_exporter
  get_url:
    url: "https://github.com/prometheus/node_exporter/releases/download/v1.9.0/node_exporter-1.9.0.linux-amd64.tar.gz"
    dest: /tmp/node_exporter.tar.gz
  when: not node_exporter_bin.stat.exists

- name: Extract node_exporter
  unarchive:
    src: /tmp/node_exporter.tar.gz
    dest: /tmp/
    remote_src: true
  when: not node_exporter_bin.stat.exists

- name: Install node_exporter binary
  copy:
    src: /tmp/node_exporter-1.9.0.linux-amd64/node_exporter
    dest: /usr/local/bin/node_exporter
    mode: "0755"
    remote_src: true
  when: not node_exporter_bin.stat.exists

- name: Create node_exporter user
  user:
    name: node_exporter
    system: true
    shell: /usr/sbin/nologin
    create_home: false

- name: Ensure node_exporter textfile directory exists
  file:
    path: /var/lib/node_exporter/textfile
    state: directory
    owner: node_exporter
    group: node_exporter
    mode: "0755"

- name: Install node_exporter systemd service
  copy:
    src: node_exporter.service
    dest: /etc/systemd/system/node_exporter.service
    mode: "0644"
  notify: restart node_exporter

- name: Enable node_exporter
  systemd:
    name: node_exporter
    enabled: true
    state: started
    daemon_reload: true
